termd 0.9.19

用户可见变化:
- 无功能变化；本版本用于完成 relay 组件自动更新的端到端验证（daemon 代理更新的 relay `/update` 链路，含代理下载）。

兼容性:
- workspace、前端和发布资产版本同步到 `0.9.19`；daemon 与 Web UI 建议同步升级，termrelay 和 termctl 可按正常版本节奏同步更新。
- 不改变配置、持久化数据、终端协议主版本或 supervisor 兼容版本（`2026-07-12-dual-ws`）。
- 普通升级不会清空或主动终止已有终端会话；现有 supervisor 和 session runtime 保持原有兼容规则。
