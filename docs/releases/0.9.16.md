termd 0.9.16

用户可见变化:
- 修复自动更新偶尔下载到旧版本的问题：GitHub `/releases/latest` API 的 CDN 缓存可能滞后几分钟，导致更新检查拿到旧 latest 并下载旧资产（表现为点击更新后版本不变）。更新检查现在携带时间戳绕过缓存。

兼容性:
- workspace、前端和发布资产版本同步到 `0.9.16`；daemon 与 Web UI 建议同步升级，termrelay 和 termctl 可按正常版本节奏同步更新。
- 本次只调整自动更新的版本检查请求，不改变配置、持久化数据、终端协议主版本或 supervisor 兼容版本（`2026-07-12-dual-ws`）。
- 普通升级不会清空或主动终止已有终端会话；现有 supervisor 和 session runtime 保持原有兼容规则。
