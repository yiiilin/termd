termd 0.9.22

用户可见变化:
- 修复经 relay 页面更新按钮仍返回 404 的问题（0.9.20 只补了转发准入匹配，未注册实际的隧道转发路由）：relay 现在把 `/api/update/*` 请求隧道转发给 daemon，经 relay 打开页面时 termd 与 relay 的「立即更新」均可正常使用。

兼容性:
- workspace、前端和发布资产版本同步到 `0.9.22`；daemon 与 Web UI 建议同步升级，termrelay 和 termctl 可按正常版本节奏同步更新。
- 本次只调整 relay 的 HTTP 隧道路由，不改变配置、持久化数据、终端协议主版本或 supervisor 兼容版本（`2026-07-12-dual-ws`）。
- 普通升级不会清空或主动终止已有终端会话；现有 supervisor 和 session runtime 保持原有兼容规则。
