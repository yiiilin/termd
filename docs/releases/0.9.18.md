termd 0.9.18

用户可见变化:
- 自动更新流程增加关键节点日志（版本检查结果、资产下载、下载后版本探测、替换），更新失败或行为异常时 daemon 日志（`journalctl -u termd`）可直接定位到具体步骤。

兼容性:
- workspace、前端和发布资产版本同步到 `0.9.18`；daemon 与 Web UI 建议同步升级，termrelay 和 termctl 可按正常版本节奏同步更新。
- 本次只增加更新流程日志，不改变配置、持久化数据、终端协议主版本或 supervisor 兼容版本（`2026-07-12-dual-ws`）。
- 普通升级不会清空或主动终止已有终端会话；现有 supervisor 和 session runtime 保持原有兼容规则。
